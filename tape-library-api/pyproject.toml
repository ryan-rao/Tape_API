[project]
name = "tape-library-api"
version = "1.0.0"
description = "REST API for Linux Tape Library management (SCSI/sg3_utils/mtx/mt) with audit and safety levels"
requires-python = ">=3.9"
dependencies = ["fastapi>=0.110", "uvicorn>=0.29", "pydantic>=2.6"]

[tool.ruff]
line-length = 120
